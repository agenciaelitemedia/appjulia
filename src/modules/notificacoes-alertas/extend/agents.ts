/**
 * extend/agents — seletor e lista de agentes da Julia (reuso do sistema).
 */
export { useJuliaAgents } from '@/pages/estrategico/hooks/useJuliaData';
export { AgentSearchSelect } from '@/components/AgentSearchSelect';
export { getSavedAgentCodes, saveAgentCodes } from '@/hooks/usePersistedPeriod';
