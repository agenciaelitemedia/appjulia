import { EquipePerformanceTab } from '../extend/equipe';

/** Aba Equipe do painel de atendimento — mesma tela de Performance do módulo Equipe. */
export function OfficeEquipeTab() {
  return <EquipePerformanceTab />;
}