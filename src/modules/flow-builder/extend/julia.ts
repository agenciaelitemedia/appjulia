/**
 * extend/julia — controle padronizado da sessão da Julia (IA/followup).
 * Reexporta o helper oficial para que o módulo não dependa do caminho interno.
 */
export * from '@/lib/juliaSessionControl';